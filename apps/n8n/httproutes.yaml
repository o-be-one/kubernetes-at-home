apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: n8n
spec:
  hostnames:
  - n8n.menia.cc
  parentRefs:
  - name: shared-gateway
    namespace: default
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    filters:
    - type: RequestRedirect
      requestRedirect:
        scheme: https
        statusCode: 301
  - backendRefs:
    - name: n8n
      port: 5678
    matches:
    - path:
        type: PathPrefix
        value: /