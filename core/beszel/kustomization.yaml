apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: beszel

resources:
  - namespace.yaml
  - beszel.yaml
  - agent.yaml
  - httproute.yaml

generators:
  - secret-generator.yaml
